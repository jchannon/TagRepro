# Repro of OpenAPI Tag bug?

Start the project and open `http://localhost:5090/swagger/v1/swagger.json` and the tag for the route will show `TagRepro` but it should show `Testing` as defined in the route.